# Next Action

## Protocol milestone

Complete the normative Evidence Bundle v0.1 draft and align every implemented field with the JSON Schemas and test vectors.

## Acceptance criteria

- every schema field has normative semantics;
- no undocumented field appears in a valid fixture;
- canonicalization vectors include Unicode, numbers, arrays, and nested objects;
- all invalid fixtures produce stable error codes;
- signatures bind protocol domain, profile, target type, and digest;
- refund demo reports are generated by the same reference verifier shipped in the repository.
